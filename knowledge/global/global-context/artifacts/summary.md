# Global — Context Notes

## Facts
- Python scripts in `src/` folders reviewed for compatibility with `python-patterns`
- Scripts and variables follow Pythonic and self-explanatory naming conventions
- Type safety ensured using `Pydantic` models for data contracts and type hints in all functions
- Issues fixed by replacing file content
